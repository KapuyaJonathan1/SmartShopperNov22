import React from 'react'; // Import React (not strictly necessary in newer React versions but still common)
import '../button.css';
// Define the component function
function CaptureButton() {
  return (
    <button>C</button>
  );
}

// Export the component to use it in other parts of the app
export default CaptureButton;